<div class="comment">
  <h4>Comment by <%= comment.author_name %></h4>
  <p><%= comment.body %></p>
  <p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</p>
  <p>Posted <%= distance_of_time_in_words_to_now(comment.created_at, Time.now) %> ago</p>
  <p>Posted <%= time_ago_in_words(comment.created_at, Time.now) %> ago</p>
</div>
